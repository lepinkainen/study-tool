services:
  anki:
    image: ghcr.io/lepinkainen/study-tool:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      DB_PATH: /data/anki.db
      PORT: "8080"
    restart: unless-stopped
